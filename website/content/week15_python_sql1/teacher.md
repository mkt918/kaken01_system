# 指導案: Week 15 「ついに合体！Pythonからデータベースを操る」

## 🎯 今週のねらいと授業のゴール
2ヶ月半かけて別々に学んできた「プログラミングロジック（Python）」と「データ保存（SQL）」を初めて合体させます。
今回のゴールは「Pythonのforループ機能を使えば、SQLで数万件のINSERT文を手書きしなくて済む！」という、プログラミングによる**「圧倒的な自動化パワー（労力削減の感動）」**を実感させることです。

---

## ⏱️ タイムテーブル (180分)

### 1️⃣ 導入：PythonとSQLの合体 (0分〜30分)
- **【解説】**: 「今まではPythonを実行し直すとレベルが1に戻ってたよね。でも今日からは、みんなが遊んでいるスマホゲームと全く同じように『セーブデータ』を作れるようになるよ！」と宣言し、生徒をワクワクさせます。
- **【演習】**: 「レッスン1」を実行させます。
- **【指導のポイント】**: 
  - プログラムを実行したフォルダ内に、本当に「〇〇.db」という神のファイルが生成される様子（エクスプローラの画面）を一緒に見てあげてください。感動します。

### 2️⃣ 呪文を自動で唱える（execute） (30分〜70分)
- **【解説】**: 「先週までは黒い画面で1行ずつSQLを手打ちしてたけど、今日はあらかじめPythonにSQLをメモさせておいて、自動で唱えさせる（executeする）よ」と説明します。
- **【演習】**: 「レッスン2」と「ドリル1-1」をやらせます。
- **【トラブルシューティング】**: 
  - `IF NOT EXISTS` を書き忘れると、2回目実行した時に `Table already exists` エラーが出ます。「なんで？1回目は成功したのに！」とパニックになるので、「もう1回その建物を建てようとしたから大工さんが怒ったんだよ」と身振り手振りで説明します。

### 3️⃣ セーブの儀式（commit） (70分〜100分) ※その後休憩10分
- **【解説】**: 銀行の振り込みエラーを例に挙げ（トランザクションの概念）、プロの世界では「全部成功するまでセーブしない（不整合を防ぐ）」ルールがあることを語ります。
- **【演習】**: 「レッスン3」を実施します。
- **【指導のポイント】**: 
  - 大半の生徒が `connection.commit()` を書き忘れて、「先生！実行できたのにデータが入ってません！」とSOSを出してきます。最高の学習機会です。「最後にセーブボタン（commit）押した？」とニヤリと笑いながら答えましょう。

### 4️⃣ 総仕上げ：ダブル・ミニプロジェクト (110分〜170分)
- **【総合演習 1】**: 「クラス名簿自動作成スクリプト」。
  - ゼロからDBを作り、テーブルを作り、3つのINSERTを実行し、commitして閉じる。この一連のライフサイクルをノーミスで作れると、大きな自信に繋がります。
- **【総合演習 2】**: 「スライム100匹・無限増殖ツール」。
  - **今週のクライマックスです。**
  - Week 6 で習った `for i in range(1, 101):` を使います。
  - `f"INSERT INTO monsters (id, name, hp) VALUES ({i}, 'スライム{i}号', 10);"`
    - ここでの注意点：文字列の「スライム〇〇号」はSQL世界では文字列なので、必ず **シングルクォーテーション（`'`）** で囲む必要があります！ `VALUES ({i}, スライム{i}号, 10)` と書いてエラーになる生徒が続出するので、黒板に赤字で『SQLの文字は、Pythonのf文字列の中でさらに「'」で囲む！』と書いてください。
  - プログラム実行後、SQLite閲覧ツール（※後述）で100件のデータがズラーッと並んでいるのを見た時の生徒の歓声はすごいです。

### 5️⃣ 小テストと振り返り (170分〜180分)
- 終わりの10分で小テストを実施し、答え合わせ。

---

## 🛠️ 教員への補足：DBファイルの中身の見方
生徒が作った `〇〇.db` は、VS Codeの標準設定では文字化けして見えません。
確認させるには以下のいずれかの方法を用意してください。
1. VS Codeの拡張機能「SQLite Viewer」を入れさせる（超おすすめ。クリックするだけで表が見えます）。
2. ブラウザで「SQLite Online」を開き、そこに生成された `game.db` をドラッグ＆ドロップさせる。

---

## 📝 小テストの解答と解説（全5問）

**Q1.** Pythonからデータベースを操作するために最初に `import` するライブラリ（連絡ツール）の名前はどれ？
- **正解:** **B. `sqlite3`**
- **解説:** スマホアプリからブラウザまで世界中で一番使われている軽量DBです。

**Q2.** まず最初に、データベースのファイルにアクセス（電話をかける）するための命令文は？
- **正解:** **C. `sqlite3.connect("db名")`**
- **解説:** コネクト（接続する）ですね。終わったら切る（close）もセットです。

**Q3.** SQLの呪文を呼び出すときに、「2度目以降の実行で『もうテーブルあるよ！』というエラー」を防ぐためにつける便利な言葉はどれ？
- **正解:** **A. `IF NOT EXISTS`**

**Q4.** Pythonで作ったSQLの呪文（CREATEやINSERT）を、実際にデータベースに向かって実行する（唱える）命令はどれ？
- **正解:** **B. `cursor.execute()`**
- **解説:** エグゼキュート＝処刑する・実行するという意味の強い単語です。

**Q5.** `INSERT` や `UPDATE` でデータを変えた後、変更を本当に確定（完全にセーブ）するために絶対に忘れてはいけない命令はどれ？
- **正解:** **B. `commit()`**
- **解説:** 「コミットする（結果にこだわる）」のコミットです。これがないとすべてパーになります。
